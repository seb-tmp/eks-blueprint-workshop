apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bootstrap-control-plane-addons
  namespace: 'argocd'
spec:
  destination:
    server: https://kubernetes.default.svc
    namespace: 'argocd'
  project: default
  source:
    path: bootstrap/control-plane/addons
    repoURL: ${repo_url}
    directory:
      recurse: true
      exclude: exclude/*
  syncPolicy:
    automated: {}
